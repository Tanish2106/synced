import { Box } from "@mui/material";

const SyncedErrorPage = (props) => {
    /*
        This Integral Page is for displaying errors like
        404, 500, etc. 
    */

    return (
        <Box sx={{ width: '100%', height: '100%', display: 'flex' }}>
            Uh Oh!
        </Box>
    );
}

export default SyncedErrorPage;